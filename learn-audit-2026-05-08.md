# /learn Audit — 2026-05-08

## What I built
A skill called /learn that audits my AI system and proposes specific improvements. When I run it, it reads every skill, file, and scheduled task in my ai-builder folder, flags gaps and contradictions, identifies tasks I'm still doing manually that could be automated, and outputs a table of proposed changes for my review.

## Proposals

| Change | Target | Type | Why |
|--------|--------|------|-----|
| Convert `email-voice-guide.docx` → Markdown and place it where the skill expects it | `humanize-writing/references/email-voice-guide.md` | Skill reference | The humanize-writing skill calls this file "mandatory" for every email. It's been missing since install. The source file exists at `docs/email-voice-guide.docx` — just needs extraction and placement. |
| Create `ai-tells.md` reference (full inventory of AI writing patterns) | `humanize-writing/references/ai-tells.md` | Skill reference | The main skill says "for the full inventory of AI tells, see `references/ai-tells.md`." That file doesn't exist. This is the systematic pass the skill relies on for long-form humanizing. |
| Create stub reference files for the 4 remaining missing voice guides | `humanize-writing/references/slack-voice-guide.md`, `cold-outreach-voice-guide.md`, `linkedin-voice-guide.md`, `social-voice-guide.md` | Skill reference | Currently the skill checks for these on every matching request and silently continues without them. Stubs prevent silent skips; full versions make the output actually tailored. |
| Create `CLAUDE.md` in workspace root | `/Users/abcause25/AI Builder Level 1/my-website/CLAUDE.md` | CLAUDE.md edit | No workspace-level context file exists. JPMorganChase context, ABC design system, AI Builder program level, and key project files (college data, personal website) are context you'd otherwise re-explain every session. |
| Clarify or remove the `./canvas-fonts` reference in canvas-design | `canvas-design/SKILL.md` | Skill edit | The skill tells Claude to "search the `./canvas-fonts` directory" for fonts, but no such directory exists in the workspace. Either create the directory with fonts or remove the reference to prevent silent failure. |
| Document or retire `email_triage_review.html` | `html/email_triage_review.html` or `email-manager/SKILL.md` | Skill edit / Memory | This review UI exists but isn't connected to anything. If it's still the workflow for reviewing triage output, the email-manager skill should reference it. If it's deprecated, document that so it doesn't create confusion. |

Status: unapproved — do not apply any changes.

## Build your own /learn skill
Paste this into Claude Cowork:

I want to save a skill in Claude Cowork called /learn.

--- name: learn version: 2.0.0 description: "Post-session retrospective: audits efficiency, proposes skill/memory/CLAUDE.md updates, and generates coaching feedback" ---  Learn from this session. Extract what went well, what went wrong, and update documentation so future sessions are better. Also give direct feedback on how the user can work with Claude more efficiently.  Parse $ARGUMENTS for optional focus area (e.g., `mistakes`, `efficiency`, `workflow`). If empty, do a full review.  ---  ### Short session check (internal — do not display)  Before doing anything else, assess the session length and complexity. If the session was short (under ~10 exchanges) or trivial (a single task, no meaningful back-and-forth, no errors or corrections), skip all steps below and output a single short paragraph instead: share whatever observation is actually worth noting from the session (if anything), and remind the user that /learn is most useful after long, complex sessions where things got messy or a lot of ground was covered.  ---  ### Step 0: Efficiency Audit (internal — do not display, use to inform proposals)  Work through each step before producing any output:  **1. Make requirements less dumb** - Were any requirements stated as facts that were actually assumptions? Who said this had to be done this way? - Did requirements come from someone who isn't going to be accountable for the outcome? - Flag any requirement that, in hindsight, was dumb, wrong, or unnecessary. **2. Delete the part/process** - What steps, outputs, or artifacts could have been eliminated entirely without losing value? - Was anything produced that won't be used? Any process run "because we always do"? - If you can't defend why something stayed, it should have been cut. **3. Simplify or optimize the design** - After deleting, what was still too complex? - Were there cleaner paths to the same result? What added steps that didn't add value? **4. Accelerate cycle time** - Where did the session slow down? Waiting on tool results, iteration loops, back-and-forth? - What would have made this 2x faster? **5. Automate** - What was done manually in this session that should be automated? (a skill, an n8n workflow, a memory update) - Only after steps 1-4 — don't automate a dumb process. ---  ### Step 1: Session Audit (internal — do not display, use to inform proposals)  Review the current conversation and identify:  **What went wrong:** - Errors, dead ends, retries, or wasted steps - Misunderstandings between user and Claude (unclear prompts, wrong assumptions) - Tools that failed or returned unexpected results - Approaches that were abandoned partway through **What went well:** - Efficient patterns (good use of parallel agents, clear prompts, smart sequencing) - Decisions that saved time or tokens - Workflows that should be repeated **What was learned:** - New facts about systems, data, processes, or tools - Corrections to previous assumptions - Patterns that should be codified ---  ### Output A (display to user)  Use `## What Was Learned` as the section header. Show 3-5 bullets from Step 1's "What was learned" findings. Reference specific moments from the session — not generic observations.  ---  ### Step 2: Documentation Updates — Skill vs. Memory Decision (internal rules — use to build Output 2)  For every insight from Steps 0 and 1, classify it:  **Update a SKILL when:** - A workflow was used and produced the wrong output or took extra steps - A missing instruction caused Claude to go off-track during a skill execution - A new edge case was discovered that a skill should handle - A skill was invoked but the description didn't match (triggering issue) **Update MEMORY when:** - A new fact was learned about a system, tool, or data source — but first check the Preferences decision tree below before defaulting to a memory file - A correction to a previous assumption (not tied to a specific skill) **Create a NEW SKILL when:** - You did something manually that took 3+ steps and will happen again - You caught yourself giving Claude the same multi-step instruction twice - A workflow emerged during the session that has a clear trigger and output **Create a SUB-AGENT when:** - A task is parallelizable — multiple independent workstreams that can run simultaneously - A task is long-running and would flood the main context window with intermediate results - A task requires autonomous tool use across many steps without needing user input mid-way - The work is isolated enough that a fresh context (no session history) is an advantage, not a liability **Skill vs. Sub-Agent decision rule:** If the work has a consistent trigger + prompt template → Skill. If the work requires autonomous execution + tool calls + parallelism → Sub-Agent. Many workflows need both.  **Preferences → where do they live?** - Specific to one workflow → `references/` inside that skill. If that skill doesn't exist yet, propose creating the skill first, then the reference file inside it - Span multiple workflows (voice, formatting, global style) → `references/` linked from CLAUDE.md - No natural skill home (org facts, constants) → standalone memory file **Update CLAUDE.md when:** - A new "always do X" or "never do Y" rule was discovered - A workspace routing rule changed - A new MCP integration was added - Global data that would clutter CLAUDE.md → create `references/` file linked from CLAUDE.md ---  ### Output B (display to user)  Use `## Documentation Proposals` as the section header. Show the proposals table:  | Change | Target | Type | Why | |--------|--------|------|-----| | {description} | {file path} | Skill edit / Skill reference / New skill / CLAUDE.md edit / CLAUDE.md reference / Memory / Sub-agent | {cite the exact step or output that makes this necessary} |  Do NOT write any changes until approved. Ask: "Want me to apply all, some, or none?"  ---  ### Step 3: User Feedback (internal — use to inform Output 3)  Identify the most important efficiency and workflow observations from Steps 0 and 1: - Were prompts unnecessarily long or missing key context? - Could work have been parallelized? - Did the user do something Claude should have done, or vice versa? - What single change would have the highest leverage on this session? ---  ### Output C (display to user)  Use `## How to Use Claude Better` as the section header. Give 2-3 specific, direct suggestions — how to prompt more efficiently, what context to front-load, or where the workflow broke down on the user's end. Do not include suggestions directed at Claude. Reference actual moments from the session. No sub-headers, no generic advice.  ---  ### Step 4: Apply Approved Changes  After the user approves (all, some, or none): - Write the approved memory files - Edit the approved skill files - Update CLAUDE.md if approved - Confirm what was updated ---  ## Notes - The documentation proposals are the durable output — they compound across future sessions - Be honest — don't soften findings - If the session went perfectly and there's nothing to learn, say so — don't manufacture feedback - Never update documentation without showing the user first and getting approval - When updating memory files, follow the memory system format (frontmatter with name, description, type) - `coaching` flag: adds full structured user feedback (token efficiency, prompt patterns, one thing to try) - `growth` flag: adds a study prompt or honest eval question at the end
